collections:
  cordex:
    collection_columns:
    - variable
    - experiment
    - global_climate_model
    - regional_climate_model
    - frequency
    - grid
    - bias_corrected_or_raw
    - store_fullpath
    order_by_columns:
      - variable
    required_columns:
      - variable
      - store_fullpath
