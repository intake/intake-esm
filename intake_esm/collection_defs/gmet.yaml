collections:
  gmet:
    collection_columns:
      - frequency
      - member_id
      - version
      - time_range
      - resolution
      - store_fullpath

    order_by_columns:
      - member_id
      - time_range
      - store_fullpath
    required_columns:
      - member_id
      - store_fullpath
