collections:
  gmet:
    collection_columns:
      - frequency
      - member_id
      - version
      - time_range
      - resolution
      - path

    order_by_columns:
      - member_id
      - time_range
      - path
    required_columns:
      - member_id
      - path
