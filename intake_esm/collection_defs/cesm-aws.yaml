collections:
  cesm-aws:
    collection_columns:
    - experiment
    - frequency
    - component
    - variable
    - store_fullpath
    order_by_columns:
      - variable
      - component
      - frequency
    required_columns:
      - store_fullpath
      - variable
      - frequency
      - component
