collections:
  cesm-aws:
    collection_columns:
    - experiment
    - frequency
    - component
    - variable
    - path
    order_by_columns:
      - variable
      - component
      - frequency
    required_columns:
      - path
      - variable
      - frequency
      - component
